{
  "name": "tangible-populater-module",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  },
  "scripts": {
    "playwright:install": "playwright install chromium"
  }
}
